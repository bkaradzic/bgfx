/*
uniform vec4 bgfx_clear_color[8];

void main()
{
	gl_FragColor = bgfx_clear_color[0];
}
*/

struct Srt {
    float4 bgfx_clear_color[8];
};

Srt const* g_srt : S_SRT_DATA;

float4 main() : S_TARGET_OUTPUT
{
	return g_srt->bgfx_clear_color[0];
}